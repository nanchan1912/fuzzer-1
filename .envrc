# direnv: auto-enter the nix dev shell when cd-ing into the repo.
#   nix profile install nixpkgs#direnv nixpkgs#nix-direnv   (once)
#   direnv allow                                            (here)
use flake

watch_file nix/shell.nix
watch_file nix/bootstrap.sh
